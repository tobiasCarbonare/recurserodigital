# Variables de entorno para el backend
# Copiar este archivo como .env para desarrollo local

# Configuración de la base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=recurseroDigitalDB
DB_USER=admindb
DB_PASSWORD=recursero2025

# Configuración del servidor
NODE_ENV=development
PORT=3000

# Integración IA (Gemini)
GEMINI_API_KEY=tu-api-key-de-gemini
GEMINI_MODEL=gemini-1.5-flash

# Configuración JWT
JWT_SECRET=tu-jwt-secret-super-seguro-aqui

# SCRIPTS DISPONIBLES:
# npm run dev          - Desarrollo con hot reload
# npm run build        - Compilar TypeScript
# npm run start:dev    - Ejecutar build compilado en modo desarrollo (sin SSL)
# npm run start        - Ejecutar build compilado en modo producción (con SSL)
F
# DOCKER COMPOSE DISPONIBLE:
# docker-compose up --build     - Desarrollo local (NODE_ENV=development, sin SSL)
# docker-compose -f docker-compose.prod.yml up --build  - Simular producción local (NODE_ENV=production, con SSL)

# Para producción configurar estas variables:
# DB_HOST=tu-host-de-postgresql-en-render
# DB_PORT=5432
# DB_NAME=tu-nombre-de-base-de-datos
# DB_USER=tu-usuario-de-base-de-datos
# DB_PASSWORD=tu-password-de-base-de-datos
# NODE_ENV=production
# JWT_SECRET=tu-jwt-secret-super-seguro-para-produccion

# IMPORTANTE: En producción, NODE_ENV=production activará automáticamente SSL/TLS
# para las conexiones a PostgreSQL
